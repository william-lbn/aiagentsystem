# A2A integration

Study Agent Card discovery, Message vs stateful Task, task states, Artifacts, streaming and cancellation. Use the official A2A CLI when available in your environment; external Agent Cards, messages and artifacts must be treated as untrusted input.

Offline object-model experiment:
```bash
PYTHONPATH=src python examples/protocol_mcp_a2a.py
```
