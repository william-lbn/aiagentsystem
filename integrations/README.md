# External integration tracks

These tracks are intentionally separated from the deterministic core. Each README states prerequisites, credentials, network access, expected commands, what is verified locally, and what remains external. Never treat installation success as experiment completion.
