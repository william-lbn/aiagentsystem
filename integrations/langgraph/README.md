# LangGraph integration

Study target: state graph, checkpointer, durable execution, interrupt/HITL and persistence.

```bash
python -m venv .venv-lg && source .venv-lg/bin/activate
pip install 'langgraph>=0.6'
python integrations/langgraph/demo.py
```
