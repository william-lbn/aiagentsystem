# OpenHands Software Agent SDK lab

OpenHands provides Python/TypeScript/REST APIs plus an Agent Server and local or ephemeral Docker/Kubernetes workspaces. Study its separation of Agent, Conversation, Tool, Workspace, events and remote Agent Server.

Follow the current official SDK quick start. For the course, execute only against the intentionally buggy fixture repository and capture the conversation/event trace plus test output before/after the patch.
